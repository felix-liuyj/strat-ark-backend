# ============================================================
# Strat Ark 后端环境变量配置示例
# 复制此文件为 .env 并填写实际值
# ============================================================

# ------------------------------------------------------------
# 应用运行配置
# ------------------------------------------------------------
APP_NAME=Strat Ark Backend API
APP_NO=strat-ark-backend
APP_ENV=local
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DEBUG=true

# ------------------------------------------------------------
# Web 跨域配置
# ------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ------------------------------------------------------------
# 数据库配置
# 仅支持 PostgreSQL，驱动固定为 postgresql+psycopg://
# ------------------------------------------------------------
DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/strat_ark
DATABASE_SCHEMA=public

# ------------------------------------------------------------
# Redis 配置
# ------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_USERNAME=default
REDIS_PASSWORD=
REDIS_SSL=false

# ------------------------------------------------------------
# JWT 配置
# ------------------------------------------------------------
JWT_SECRET_KEY=replace-with-a-secure-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7
JWT_REFRESH_EXPIRE_DAYS=30
# 管理员邮箱后缀，多个用逗号分隔，例如 example.com,admin.example.com
ADMIN_EMAIL_SUFFIXES=

# ------------------------------------------------------------
# OAuth 登录配置
# Public PKCE + 后端 exchange；前端也需要配置同一个 client id
# ------------------------------------------------------------
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_MICROSOFT_CLIENT_ID=
OAUTH_MICROSOFT_TENANT=common
OAUTH_TOKEN_TIMEOUT_SECONDS=10

# ------------------------------------------------------------
# 业务敏感数据加密配置
# 交易所 / 网关 API Key 入库加密使用；生产必须由运行环境注入强随机值
# ------------------------------------------------------------
ENCRYPT_KEY=

# ------------------------------------------------------------
# 阿里云 OSS 配置
# ACCESS_KEY/SECRET 建议由运行环境注入，不提交至版本库
# ALI_OSS_REGION 支持 `cn-shanghai`、`oss-cn-shanghai` 或完整 endpoint
# ------------------------------------------------------------
ALI_OSS_ACCESS_KEY=
ALI_OSS_ACCESS_SECRET=
ALI_OSS_REGION=
ALI_OSS_BUCKET_NAME=
BRAND_LOGO_OSS_PATH=

# ------------------------------------------------------------
# SMTP 邮件配置
# ------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_SENDER=
SMTP_USE_SSL=true

# ------------------------------------------------------------
# 行情数据源
# 留空即使用 libs/integrations/* stub，不影响本地运行与演示
# ------------------------------------------------------------
MARKET_DATA_WS_URL=
MARKET_DATA_API_KEY=

# ------------------------------------------------------------
# 引擎集群运维（Kubernetes）
# ------------------------------------------------------------
K8S_NAMESPACE=stratark-prod
K8S_IN_CLUSTER=false

# ------------------------------------------------------------
# 静态资源配置
# ------------------------------------------------------------
STATIC_DIR=./statics
STATIC_URL=/statics
