# 敏感配置绝不进镜像层（运行时经 compose environment / EnvironmentFile 注入）
.env
.env.*
!.env.example

# 版本控制与本地环境
.git
.gitignore
.venv
venv

# Python 产物
__pycache__
*.pyc
*.pyo
.ruff_cache
.pytest_cache

# 部署与编排资产（容器不需要；编排器 / 前端有各自的构建上下文）
deploy/
docs/
engines/
docker-compose*.yml
